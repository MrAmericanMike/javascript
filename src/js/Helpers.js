// You can export multiple functions from a file
// This you import in another file this way:
// import { add, subtract } from "./Helpers.js";

export function add(a, b) {
	return a + b;
}

export function subtract(a, b) {
	return a - b;
}
